<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="users">

	<insert id="insert" parameterType="usersDto">

	INSERT INTO users (id, pwd, name, email, phone, post, addr, detailaddr, regdate)
	VALUES (#{id}, #{pwd},#{name}, #{email}, #{phone}, #{post}, #{addr}, #{detailaddr}, sysdate())

	</insert>

	<select id="getData" parameterType="String" resultType="usersDto">
		SELECT num, id, pwd, name, email, phone, post, addr, detailaddr, regdate FROM users where id=#{id}
	</select>
</mapper>